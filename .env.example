# TigerGraph Savanna workspace
# Savanna -> your workspace -> Query Editor -> the link icon -> Connect From API
TG_HOST=https://tg-<workspace-id>.tg-<org>.i.tgcloud.io
# Savanna -> Database Secrets -> Create Secret
TG_SECRET=
TG_GRAPH=Fraud_Investigation

# Optional: an LLM for narrative polish. The pipeline runs fully without one.
# GEMINI_API_KEY=
